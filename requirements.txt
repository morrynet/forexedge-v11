# Core web framework
flask>=3.0.0
gunicorn>=21.2.0
werkzeug>=3.0.0        # password hashing (check_password_hash / generate_password_hash)

# Database
psycopg2-binary>=2.9.9  # PostgreSQL (used when DATABASE_URL is set)

# Rate limiting shared store
redis>=5.0.0            # optional but required for multi-worker rate limiting

# Scheduler (monthly referral audit emails)
APScheduler>=3.10.0

# Telegram bots
python-telegram-bot>=20.7
telethon>=1.34.0        # userbot

# HTTP / AI
requests>=2.31.0
anthropic>=0.25.0

# Testing
pytest>=8.0.0
pytest-flask>=1.3.0
